# @goldshore/theme

This package contains the design tokens and global styles for the GoldShore UI System.

## Usage

Import the theme in your application entry point:

```css
import '@goldshore/theme';
```

## Structure

- `tokens.css`: CSS variables for colors, spacing, etc.
- `reset.css`: Minimal CSS reset.
- `index.css`: Main entry point.
