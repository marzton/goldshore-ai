# Workflow Map & Inventory (Phase 0)

This document maps GitHub Actions workflows in the repository, their triggers, dependencies, and current on-disk status.
Generated by Jules (Phase 0: Containment).

## Workflow Inventory

| Workflow Name | File Path | Trigger | Dependencies | Reusable Refs | Status | Notes |
| :--- | :--- | :--- | :--- | :--- | :--- | :--- |
| Deploy GS Admin | `.github/workflows/deploy-gs-admin.yml` | `push` (main, path-filtered) | `actions/checkout`, `actions/setup-node`, Cloudflare Pages action | None | VALID | Deploys to Cloudflare Pages. |
| Deploy GS Agent | `.github/workflows/deploy-gs-agent.yml` | `pull_request` (path-filtered, production label / ready-for-review gate) | `actions/checkout`, `pnpm/action-setup`, `actions/setup-node` | None | VALID | Active on disk; despite the name, it is PR-gated rather than `push`-driven. |
| Deploy GS API | `.github/workflows/deploy-gs-api.yml` | `push` (main, path-filtered) | `actions/checkout`, `actions/setup-node` | None | VALID | Deploys using Wrangler. |
| Deploy GS Control | `.github/workflows/deploy-gs-control.yml.disabled` | Disabled on disk (`.disabled`) | `actions/checkout`, `pnpm/action-setup`, `actions/setup-node` | None | **DISABLED** | Keep marked disabled until the file is renamed back to `.yml`. |
| Deploy GS Gateway | `.github/workflows/deploy-gs-gateway.yml.disabled` | Disabled on disk (`.disabled`) | `actions/checkout`, `pnpm/action-setup`, `actions/setup-node` | None | **DISABLED** | Keep marked disabled until the file is renamed back to `.yml`. |
| Deploy GS Mail | `.github/workflows/deploy-gs-mail.yml` | `push` (main, path-filtered) | `actions/checkout`, `pnpm/action-setup`, `actions/setup-node` | None | VALID | Deploys using Wrangler. |
| Deploy GS Web | `.github/workflows/deploy-gs-web.yml` | `push` (main, path-filtered) | `actions/checkout`, `pnpm/action-setup`, `actions/setup-node`, Cloudflare Pages action | None | VALID | Deploys to Cloudflare Pages. |
| Jules Nightly | `.github/workflows/jules-nightly.yml` | `schedule`, `workflow_dispatch` | Reusable workflow calls | `jules-daily.yml`, `jules-nightly-clean.yml`, `palette-daily.yml`, `sentinel-nightly.yml`, `conflict-sweeper-nightly.yml` | **INVALID** | Active file still references reusable workflows that are not present. |
| Lockfile Guard | `.github/workflows/lockfile-guard.yml` | `pull_request`, `workflow_dispatch` | `actions/checkout` | None | VALID | Blocks lockfile drift in PRs and supports manual checks. |
| Maintenance: GS Sync | `.github/workflows/maintenance-gs-sync.yml` | `workflow_dispatch` | Repository-local maintenance steps | None | VALID | Manual GS sync maintenance workflow. |
| Maintenance: Cloudflare Infra Reconcile | `.github/workflows/maintenance.yml` | `workflow_dispatch` | `actions/checkout`, `actions/setup-node` | None | VALID | Manual Cloudflare KV/system reconciliation after drift, rotation, or incident response. |
| Naming Guard | `.github/workflows/naming-guard.yml` | `workflow_dispatch` | `actions/checkout` | None | VALID | Manual workflow naming/path guard. |
| Naming Lint | `.github/workflows/naming-lint.yml` | `pull_request`, `push` | `actions/checkout`, `pnpm/action-setup`, `actions/setup-node` | None | VALID | Runs `pnpm check:naming`. |
| NeuraLegion Scan | `.github/workflows/neuralegion.yml` | `schedule`, `workflow_dispatch`, `push` | `actions/checkout`, `NeuraLegion/run-scan` | None | VALID | Security scan workflow. |
| Palette Dispatch Run | `.github/workflows/palette-manual.yml` | `repository_dispatch` | `actions/checkout`, `actions/setup-node` | None | VALID | Dispatch-triggered Palette agent run. |
| PII Scan | `.github/workflows/pii-scan.yml` | `workflow_dispatch` | `actions/checkout`, `actions/setup-node`, `actions/upload-artifact` | None | VALID | Runs `pnpm scan:pii`. |
| Preview GS Admin | `.github/workflows/preview-gs-admin.yml` | `pull_request` (path-filtered, preview label / ready-for-review gate) | `actions/checkout`, `actions/setup-node`, Cloudflare Pages action | None | VALID | Deploys preview to Cloudflare Pages. |
| Preview GS Agent | `.github/workflows/preview-gs-agent.yml` | `pull_request` (path-filtered, preview label / ready-for-review gate) | `actions/checkout`, `pnpm/action-setup`, `actions/setup-node` | None | VALID | Deploys preview using Wrangler. |
| Preview GS API | `.github/workflows/preview-gs-api.yml` | `pull_request` (path-filtered, preview label / ready-for-review gate) | `actions/checkout`, `actions/setup-node` | None | VALID | Deploys preview using Wrangler. |
| Preview GS Gateway | `.github/workflows/preview-gs-gateway.yml` | `pull_request` (path-filtered, preview label / ready-for-review gate) | `actions/checkout`, `pnpm/action-setup`, `actions/setup-node` | None | VALID | Deploys preview using Wrangler. |
| Preview GS Web | `.github/workflows/preview-gs-web.yml` | `pull_request` (path-filtered, preview label / ready-for-review gate) | `actions/checkout`, `pnpm/action-setup`, `actions/setup-node`, Cloudflare Pages action | None | VALID | Deploys preview to Cloudflare Pages. |
| Repo Health | `.github/workflows/repo-health.yml` | `pull_request`, `workflow_dispatch` | Repository-local health checks | None | VALID | Audits repo hygiene and consistency. |
| Route Collision Check | `.github/workflows/route-collision-check.yml` | `pull_request`, `workflow_dispatch` | `actions/checkout`, `pnpm/action-setup`, `actions/setup-node` | None | VALID | Runs route collision check. |
| Signed Commit Guard | `.github/workflows/signed-commit-guard.yml` | `pull_request`, `workflow_dispatch` | Repository-local signature checks | None | VALID | Verifies commit signing policy. |
| SonarCloud | `.github/workflows/sonarcloud.yml` | `push`, `pull_request`, `workflow_dispatch`, `schedule` | `SonarSource/sonarcloud-github-action` | None | VALID | Uses SonarCloud action. |
| Stabilization Task | `.github/workflows/stabilization-task.yml` | `schedule`, `workflow_dispatch` | `actions/checkout`, `actions/setup-node`, `stefanzweifel/git-auto-commit-action` | None | VALID | Runs stabilization checks. |
| Summary | `.github/workflows/summary.yml` | `issues` | `actions/checkout`, `actions/ai-inference` | None | VALID | Comments an AI-generated summary on newly opened issues. |
| TFSec | `.github/workflows/tfsec.yml` | `push`, `pull_request`, `schedule` | `actions/checkout`, `aquasecurity/tfsec-sarif-action`, `github/codeql-action` | None | VALID | Runs infrastructure security scanning. |

## Cross-Repo References

No cross-repository workflow triggers (for example `repository_dispatch` targeting other repos) were identified.
External actions are pinned or versioned appropriately, except for:
* `actions/ai-inference`: uses `v2`; verify origin/validity as part of ongoing governance review.

## Governance Observations

1. **Broken orchestration references**: `jules-nightly.yml` remains active but still calls reusable workflows that are missing from `.github/workflows/`.
2. **Disabled deploys are explicit on disk**: `deploy-gs-control` and `deploy-gs-gateway` are disabled via `.disabled` suffixes rather than active `.yml` files.
3. **Strict lockfile policy**: `lockfile-guard.yml` prevents lockfile changes in PRs without process.
4. **AI issue summaries**: `summary.yml` uses `actions/ai-inference@v2` and should remain under cost/governance review.

## Next Steps (Phase 1)

* Delete or repair `jules-nightly.yml`.
* Review `summary.yml` governance/cost posture.
* Re-enable disabled deploy workflows only by renaming the corresponding `.disabled` files back to `.yml`.
