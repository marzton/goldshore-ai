name: Inventory preview route ownership

# Read-only drift inventory. Cloudflare production configuration is changed only
# by an approved human in the dashboard; this workflow has no Cloudflare credentials.
on:
  pull_request:
    paths:
      - 'apps/gs-web/wrangler.toml'
      - 'apps/gs-api/wrangler.toml'
      - 'infra/Cloudflare/**'
      - 'scripts/export-cloudflare-inventory.py'
      - '.github/workflows/setup-preview-dns.yml'
  push:
    branches: [main]
    paths:
      - 'apps/gs-web/wrangler.toml'
      - 'apps/gs-api/wrangler.toml'
      - 'infra/Cloudflare/**'
      - 'scripts/export-cloudflare-inventory.py'
      - '.github/workflows/setup-preview-dns.yml'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  inventory:
    name: Export redacted expected state
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7

      - uses: actions/setup-node@v7.0.0
        with:
          name: cloudflare-inventory-${{ github.run_id }}-setup-preview-dns
          path: cloudflare-inventory.json
          retention-days: 30
