# Legacy Cloudflare Wrangler manifests

Files in this folder are archived `goldshore-*` Wrangler manifests kept only for historical reference.

- They are **not canonical**.
- They are **not deployable**.
- Active deployments must use the canonical `gs-*` manifest paths documented in `infra/Cloudflare/README.md`.
