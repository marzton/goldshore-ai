# Deprecated shim: the agent worker was renamed to `gs-agent`.
# Keep this file in sync with `gs-agent.wrangler.toml` so workflows or
# scripts referencing the legacy path continue to work.
name = "gs-agent"
main = "../../apps/gs-agent/src/index.ts"
compatibility_date = "2024-03-20"
tsconfig = "../../apps/gs-agent/tsconfig.json"

[[queues.consumers]]
  queue = "goldshore-jobs"

[env.dev]
name = "gs-agent-dev"
# Dev bindings for the agent
# [[queues.producers]]
# binding = "QUEUES_JOBS"
# queue = "goldshore-jobs-dev"

[env.preview]
name = "gs-agent-preview"
# Preview bindings for the agent

[env.prod]
name = "gs-agent-prod"
# Production bindings for the agent
