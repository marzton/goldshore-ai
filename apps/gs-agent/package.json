{
  "name": "@goldshore/gs-agent",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "dev": "wrangler dev --config wrangler.toml",
    "deploy": "wrangler deploy --minify --env prod --config wrangler.toml",
    "deploy:preview": "wrangler deploy --env preview --config wrangler.toml",
    "build": "wrangler deploy --env prod --dry-run --outdir=dist --config wrangler.toml",
    "test": "tsx src/index.test.ts"
  },
  "dependencies": {
    "@goldshore/auth": "workspace:*",
    "hono": "^4.12.3"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260301.1",
    "typescript": "^5.4.0",
    "wrangler": "^4.75.0"
  }
}
