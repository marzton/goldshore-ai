{
  "name": "@goldshore/agent",
  "version": "0.0.1",
  "private": true,
  "main": "src/index.ts",
  "scripts": {
    "dev": "wrangler dev src/index.ts --config wrangler.toml",
    "deploy": "wrangler deploy --minify src/index.ts --config wrangler.toml",
    "build": "wrangler deploy src/index.ts --dry-run --outdir=dist --config wrangler.toml"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260214.0",
    "typescript": "^5.0.0",
    "wrangler": "^4.65.0"
  }
}
