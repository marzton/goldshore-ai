# legacy goldshore-api

## Overview

Legacy Cloudflare Worker configuration retained for historical reference.

## Notes

- Current API development lives in `apps/api-worker`.
- This directory includes only a legacy `wrangler.toml`.
