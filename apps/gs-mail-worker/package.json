{
  "name": "@goldshore/gs-mail-worker",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev src/index.ts",
    "deploy": "wrangler deploy",
    "build": "wrangler deploy --dry-run --outdir=dist"
  },
  "dependencies": {
    "hono": "^4.0.0",
    "postal-mime": "^2.2.0"
  },
  "devDependencies": {
    "wrangler": "^3.101.0",
    "@cloudflare/workers-types": "^4.20241106.0",
    "typescript": "^5.4.0"
  }
}
