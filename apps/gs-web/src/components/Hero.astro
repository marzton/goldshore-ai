<section class="codex-hero hero-section" aria-label="Gold Shore Living Codex">
  <canvas id="hero-stars" aria-hidden="true"></canvas>

  <div class="hero-inner">
    <p class="eyebrow">Steering Capital</p>
    <h1>Shaping Waves with Applied Intelligence</h1>
    <p class="subtext">Institutional-grade automation and infrastructure.</p>
    <button class="briefing-trigger" type="button">Request Briefing</button>

    <div class="node-grid workflows" aria-label="Workflow intelligence nodes">
      <article class="glass-card intelligence-node" data-tilt>
        <h2>Signal Intake</h2>
        <p>Capture operator prompts, lead context, and market triggers into auditable streams.</p>
      </article>
      <article class="glass-card intelligence-node" data-tilt>
        <h2>Execution Engine</h2>
        <p>Route policies across automation rails with guardrails for compliance and latency.</p>
      </article>
      <article class="glass-card intelligence-node" data-tilt>
        <h2>Review Loop</h2>
        <p>Close the loop with explainable outcomes, attribution, and replay diagnostics.</p>
      </article>
    </div>

    <div class="services-strip" aria-label="Service modules">
      <article class="glass-card">AI Platform Ops</article>
      <article class="glass-card">Risk Radar Monitoring</article>
      <article class="glass-card">Data Infrastructure</article>
      <article class="glass-card">Governance Automation</article>
    </div>

    <section class="risk-radar glass-card" aria-label="Risk radar HUD">
      <h2>Risk Radar</h2>
      <p>LATENCY 24ms · UPTIME 99.98% · WATCHLIST ACTIVE · SYSTEM STATUS GREEN</p>
    </section>
  </div>

  <dialog class="briefing-modal" data-briefing-modal>
    <form method="dialog" class="briefing-form">
      <header>
        <h3>Request Briefing</h3>
        <button type="button" data-close-briefing aria-label="Close briefing">×</button>
      </header>
      <label>
        Name
        <input type="text" required />
      </label>
      <label>
        Email
        <input type="email" required />
      </label>
      <label>
        Objective
        <textarea rows="3" placeholder="What operating system should we shape?"></textarea>
      </label>
      <div class="modal-actions">
        <button type="button" data-close-briefing>Cancel</button>
        <button type="submit">Transmit</button>
      </div>
    </form>
  </dialog>
</section>

<style>
  .hero-section {
    --obsidian: #020203;
    --gold-glow: rgba(212, 175, 55, 0.35);
    position: relative;
    overflow: hidden;
    border-radius: 1.25rem;
    background: radial-gradient(circle at center, #0a0a0f 0%, var(--obsidian) 100%);
    border: 1px solid rgba(255, 255, 255, 0.08);
    padding: clamp(2rem, 6vw, 4rem);
  }
  #hero-stars {
    position: absolute;
    inset: 0;
    width: 100%;
    height: 100%;
    z-index: 0;
  }
  .hero-inner {
    position: relative;
    z-index: 1;
    display: grid;
    gap: 1.25rem;
  }
  .eyebrow {
    margin: 0;
    color: #d6b65a;
    font-family: 'JetBrains Mono', monospace;
    font-size: 0.78rem;
    letter-spacing: 0.2em;
    text-transform: uppercase;
  }
  h1 {
    margin: 0;
    color: #f8fafc;
    font-size: clamp(2rem, 5vw, 3.6rem);
    font-family: 'Playfair Display', Georgia, serif;
  }
  .subtext { margin: 0; color: #dbe3f3; font-size: clamp(1rem, 2.1vw, 1.2rem); }
  .briefing-trigger {
    justify-self: start;
    border: 1px solid rgba(255, 255, 255, 0.25);
    background: rgba(255, 255, 255, 0.08);
    backdrop-filter: blur(12px);
    color: #fff;
    border-radius: 999px;
    padding: 0.7rem 1.2rem;
    cursor: pointer;
    transition: box-shadow .25s ease;
  }
  .briefing-trigger:hover { box-shadow: 0 0 20px var(--gold-glow); }

  .node-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(180px, 1fr));
    gap: 1rem;
  }

  .glass-card {
    background: rgba(255, 255, 255, 0.03);
    backdrop-filter: blur(15px);
    border: 1px solid rgba(255, 255, 255, 0.1);
    border-radius: 0.9rem;
    padding: 1rem;
    color: #e5e7eb;
  }
  .intelligence-node h2 { margin: 0 0 .5rem; font-size: 1rem; }
  .intelligence-node p { margin: 0; font-size: .92rem; }

  .services-strip {
    display: grid;
    grid-auto-flow: column;
    grid-auto-columns: minmax(180px, 1fr);
    overflow-x: auto;
    gap: 0.75rem;
    scroll-snap-type: x mandatory;
  }
  .services-strip > * { scroll-snap-align: start; }

  .risk-radar {
    background: #07090f;
    font-family: 'Fira Code', 'JetBrains Mono', monospace;
    letter-spacing: 0.05em;
  }

  .briefing-modal {
    border: 1px solid rgba(255, 255, 255, 0.18);
    border-radius: 1rem;
    background: #0a0d16;
    color: #f8fafc;
    width: min(520px, 90vw);
  }
  .briefing-form { display: grid; gap: .8rem; }
  .briefing-form header { display: flex; align-items: center; justify-content: space-between; }
  .briefing-form label { display: grid; gap: .25rem; font-size: .9rem; }
  .briefing-form input,
  .briefing-form textarea {
    background: rgba(255, 255, 255, 0.05);
    border: 1px solid rgba(255, 255, 255, 0.2);
    color: inherit;
    border-radius: .45rem;
    padding: .5rem;
  }
  .modal-actions { display: flex; justify-content: flex-end; gap: .75rem; }
</style>
