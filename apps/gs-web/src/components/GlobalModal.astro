<div class="gs-modal" data-gs-modal>
  <div class="gs-modal-backdrop" data-gs-modal-backdrop></div>
  <div
    class="gs-modal-panel gs-mirror-frame frosted-glass"
    role="dialog"
    aria-modal="true"
    aria-labelledby="gs-modal-title"
    aria-describedby="gs-modal-description"
    tabindex="-1"
  >
    <button
      class="gs-modal-close gs-icon-btn"
      data-gs-modal-close
      aria-label="Close">×</button
    >
    <div class="gs-modal-body" data-gs-modal-body></div>
  </div>
</div>

<style>
  .gs-modal {
    position: fixed;
    inset: 0;
    display: grid;
    place-items: center;
    opacity: 0;
    pointer-events: none;
    transition: opacity var(--gs-mid) var(--gs-ease);
    z-index: 80;
  }
  .gs-modal.is-open {
    opacity: 1;
    pointer-events: auto;
  }
  .gs-modal-backdrop {
    position: absolute;
    inset: 0;
    background: rgba(3, 8, 21, 0.66);
    backdrop-filter: blur(12px) saturate(130%);
  }
  .gs-modal-panel {
    position: relative;
    width: min(620px, 92vw);
    border: 1px solid rgba(255, 255, 255, 0.22);
    border-radius: 18px;
    background: linear-gradient(
      160deg,
      rgba(255, 255, 255, 0.14),
      rgba(255, 255, 255, 0.06)
    );
    box-shadow: 0 30px 80px rgba(2, 6, 23, 0.58);
    backdrop-filter: blur(22px) saturate(155%);
    -webkit-backdrop-filter: blur(22px) saturate(155%);
    padding: 1.7rem;
    transform: translateY(26px);
    transition: transform var(--gs-mid) var(--gs-ease);
    z-index: 1;
  }

  .frosted-glass {
    backdrop-filter: blur(12px) saturate(180%);
    -webkit-backdrop-filter: blur(12px) saturate(180%);
    background-color: rgba(17, 25, 40, 0.75);
    border: 1px solid rgba(255, 255, 255, 0.125);
    border-radius: 12px;
    box-shadow: 0 8px 32px 0 rgba(0, 0, 0, 0.37);
  }

  @supports not ((backdrop-filter: blur(1px)) or (-webkit-backdrop-filter: blur(1px))) {
    .frosted-glass {
      background-color: rgba(17, 25, 40, 0.9);
    }
  }
  .gs-modal.is-open .gs-modal-panel { transform: translateY(0); }
  .gs-modal-close { position: absolute; top: 14px; right: 14px; }
  .gs-modal-title { margin-top: 0.2rem; }
  .gs-modal.is-open .gs-modal-panel {
    transform: translateY(0);
  }
  .gs-modal-close {
    position: absolute;
    top: 14px;
    right: 14px;
  }
  .gs-modal-title {
    margin-top: 0.2rem;
  }
</style>
