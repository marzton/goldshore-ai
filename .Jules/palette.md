## 2026-03-07 - Dynamic Status Accessibility\n**Learning:** Dynamic status indicators (like API connectivity status or health checks) that update asynchronously need `aria-live="polite"` so screen readers announce changes without interrupting the user.\n**Action:** Always add `aria-live` to containers whose content updates dynamically based on user interaction or network events.
