# PR review outcomes — 3338, 3535, 3582, 3860

This review records the required disposition for each stale PR branch.

## Outcome summary

| PR    | Outcome                     | Rationale                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| ----- | --------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| #3338 | Close as stale / superseded | The branch mixes workflow rewrites, hero/layout experiments, scripts, and docs across 120 files. No single still-wanted behavior was isolated without re-reviewing a large stale bundle, so the old branch should not be merged.                                                                                                                                                                                                                                                                                 |
| #3535 | Recreated on fresh branch   | Keep a scoped GS Web intake change only: send `Request Briefing` CTAs to `/contact?inquiry=strategy-call`, expose an `Inquiry type` selector on the contact form, and preselect `Strategy call` when that query string is present. Files: `apps/gs-web/src/components/SiteNav.astro`, `apps/gs-web/src/pages/contact.astro`, `apps/gs-web/tests/e2e/public-pages.spec.ts`. User-visible behavior: visitors who click `Request Briefing` land on the contact form with the correct inquiry type already selected. |
| #3582 | Recreated on fresh branch   | Keep a scoped theming change only: centralize homepage hero and divider effect colors into shared theme tokens. Files: `packages/theme/src/styles/tokens.css`, `apps/gs-web/src/styles/home.css`. User-visible behavior: the homepage keeps the same visual treatment while hero highlight, divider, and panel colors come from reusable theme variables instead of hardcoded literals.                                                                                                                          |
| #3860 | Close as stale / superseded | The branch spans hundreds of files across workflows, docs, runtime code, assets, and large web IA changes. No concrete product-approved slice was narrow enough to justify reviving from the stale branch, so it should be closed instead of merged.                                                                                                                                                                                                                                                             |

## Notes for maintainers / product owners

- Do not merge any of the old PR branches directly.
- If additional behavior from `#3338` or `#3860` is still wanted, open a new targeted request from current `main` with explicit file paths and expected UX/CI outcomes before implementation.
- The recreated work above is intentionally limited to the two scoped behaviors that were concrete enough to preserve without reviving stale branch drift.
