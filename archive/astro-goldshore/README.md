# Archived: astro-goldshore

This directory contains the legacy standalone astro-goldshore workspace.

- Archived on: 2026-02-24
- Reason: `goldshore-ai/` is now the single active source of truth.
- Migration mapping and decisions: see `reports/migration/path-map.tsv` and related files under `reports/migration/`.
- Active replacements live under `apps/`, `packages/`, `infra/Cloudflare/`, `.github/`, and `public/assets/`.

This archive is read-only reference material and excluded from active build graph.
